package auth

import (
	"testing"
)

func TestAuthInterceptor(t *testing.T) {
	// jwtManager := NewJWTManager("secret-test", 30 * time.Minute)
	// interceptor := AuthInterceptor{JwtManager: *jwtManager}
	// token, err := testutils.GenerateJWTKey()
	// if err != nil {
	//     t.Errorf("Error while generating token from utils: %v", err)
	// }

	// interceptor.authorize()
}
